action (search_by_time) {
  description (search opening hospital/pharmacy at specific time)
  collect {
    input (__INPUT_NAME__) {
      type (__INPUT_TYPE__)
      min (Optional) max (One)
    }
  }
  output (__OUTPUT__)
}
