action (search_by_place) {
  description (search opening hospital/pharmacy in specific place)
  collect {
    input (__INPUT_NAME__) {
      type (__INPUT_TYPE__)
      min (Optional) max (One)
    }
  }
  output (__OUTPUT__)
}
