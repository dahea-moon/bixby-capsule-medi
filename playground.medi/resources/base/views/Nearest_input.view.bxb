input-view {
  match: Nearests (this) {
    from-output: searchNearest
  }

  message (선택)

  render {
    selection-of (this) {
      where-each (one) {
        layout-macro (nearest_mapcard) {
          param (info) {
            expression (one)
          }
        }
      }
    }
  }
}